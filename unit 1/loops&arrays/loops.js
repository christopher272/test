var officeItems = ["stapler", "monitor", "computer", "desk", "lamp", "computer", "lamp", "stapler", "computer",  "computer"];
var officeData = ["0"];
for(var i = 0; i < officeItems.length; i++){
    if(officeItems[i] === "computer"){officeData ++;}
   
 }
 console.log(officeData);